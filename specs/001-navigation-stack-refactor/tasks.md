# Tasks: Рефакторинг навигационного стека

**Входные данные**: Артефакты дизайна из `specs/001-navigation-stack-refactor/`
**Предварительные условия**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅

**Тесты**: Тестовый фреймворк не настроен (см. CLAUDE.md). Верификация — ручная (`npm run build` + ручной прогон навигации).

**Организация**: Задачи сгруппированы по пользовательским историям для независимой реализации и проверки каждой истории.

## Формат: `[ID] [Story] Описание`

- **[Story]**: К какой пользовательской истории относится задача (US1, US2)
- В описаниях указаны точные пути к файлам

---

## Фаза 1: Подготовка (Общая инфраструктура)

**Цель**: Убедиться, что рабочее окружение готово к рефакторингу

- [x] T001 Проверить, что `npm run build` проходит без ошибок в текущем состоянии (базовая линия)

---

## Фаза 2: Основа (Блокирующие предпосылки)

**Цель**: Создать новый модуль навигационного стека до того, как будут обновлены импорты

**⚠️ КРИТИЧНО**: Фазы US1 и US2 не могут начаться до завершения этой фазы

- [x] T002 Создать `src/utils/navigationStack.ts`: перенести константу `MAX_NAV_USERS`, переменную `userNavigationStack`, функцию `trimNavigationStack` и экспортировать `pushToStack`, `popFromStack`, `clearUserNavigationStack` с JSDoc-документацией на русском

**Чекпоинт**: Новый модуль создан и компилируется — можно приступать к историям

---

## Фаза 3: Пользовательская история 1 — Импорт из отдельного модуля (Приоритет: P1) 🎯 MVP

**Цель**: Убрать логику стека из точки входа и обновить импорты в потребляющих файлах

**Независимый тест**: `grep -n "pushToStack\|popFromStack\|clearUserNavigationStack\|userNavigationStack\|trimNavigationStack\|MAX_NAV_USERS" src/index.ts` возвращает 0 результатов; `grep "from './navigationStack'" src/utils/handlers.ts` находит строку импорта

### Реализация для Пользовательской истории 1

- [ ] T003 [US1] Удалить из `src/index.ts`: объявление `MAX_NAV_USERS`, переменную `userNavigationStack`, функцию `trimNavigationStack`, экспорты `pushToStack`, `popFromStack`, `clearUserNavigationStack`
- [ ] T004 [US1] Обновить импорт в `src/utils/handlers.ts`: заменить `from '..'` на `from './navigationStack'`

**Чекпоинт**: Пользовательская история 1 завершена — `npm run build` проходит без ошибок, все импорты указывают на `utils/navigationStack`

---

## Фаза 4: Пользовательская история 2 — Навигация без регрессий (Приоритет: P2)

**Цель**: Убедиться, что поведение бота с точки зрения пользователя не изменилось

**Независимый тест**: Запустить бота и вручную пройти `/start → выбор группы → назад → назад`; главное меню отображается корректно на каждом шаге

### Реализация для Пользовательской истории 2

- [ ] T005 [US2] Запустить `npm run build` и убедиться, что компиляция TypeScript проходит без ошибок
- [ ] T006 [US2] Проверить вручную сценарий навигации: `/start` → кнопка уровня 1 → кнопка уровня 2 → «Назад» × 2 → возврат к главному меню

**Чекпоинт**: Обе пользовательские истории завершены — рефакторинг структурно корректен и функционально эквивалентен

---

## Фаза 5: Полировка

**Цель**: Финальная проверка качества кода

- [ ] T007 Запустить `cd admin && npm run lint` и убедиться в отсутствии ошибок (линтер бота не настроен)
- [ ] T008 Обновить строку в `CLAUDE.md` (`handlers.ts imports pushToStack/popFromStack from index.ts`) — изменить на корректный путь `utils/navigationStack`

---

## Зависимости и порядок выполнения

### Зависимости фаз

- **Подготовка (Фаза 1)**: Нет зависимостей — можно начинать немедленно
- **Основа (Фаза 2)**: Зависит от завершения Фазы 1 — БЛОКИРУЕТ обе истории
- **US1 (Фаза 3)**: Зависит от завершения Фазы 2
- **US2 (Фаза 4)**: Зависит от завершения Фазы 3 (нужен компилируемый код для ручной проверки)
- **Полировка (Фаза 5)**: Зависит от завершения обеих историй

### Зависимости внутри историй

- T003 и T004 можно выполнять в любом порядке после T002 — они не модифицируют один и тот же файл

---

## Стратегия реализации

### MVP (только Пользовательская история 1)

1. Завершить Фазу 1: Подготовка (T001)
2. Завершить Фазу 2: Основа (T002)
3. Завершить Фазу 3: US1 (T003, T004)
4. **ОСТАНОВИТЬСЯ И ПРОВЕРИТЬ**: `npm run build` проходит, `grep` показывает корректные импорты
5. Деплой/демо если готово

### Полная поставка

1. Завершить MVP → US1 готова
2. Завершить US2 (T005, T006) → Поведение подтверждено
3. Завершить Полировку (T007, T008) → Кодовая база чистая
4. Создать PR в `main`

---

## Примечания

- Метка [Story] связывает задачу с конкретной пользовательской историей для отслеживания
- T002 — единственная по-настоящему блокирующая задача: создание модуля до обновления импортов
- Рефакторинг сугубо структурный: 1 новый файл, 2 изменённых файла, нет изменений API или поведения
